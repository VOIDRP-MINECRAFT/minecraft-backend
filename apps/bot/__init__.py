"""VoidRP Telegram bot (aiogram 3).

Runs as a separate long-polling process, sharing the backend's database,
services (NewsService) and configuration (proxy, token). Start with:

    python -m apps.bot.main
"""
